# AngenticLanggraph
Agentic LangGraph — Overview Agentic LangGraph means building autonomous AI agents using LangGraph: a framework for stateful, multi-step workflows where an LLM can think → act (tools) → observe → repeat until the task is done.  It fits naturally after your LangChain notebooks (especially tools, messages, and middleware).
